namespace Pages
{
    public static class Constants
    {
        /// <summary>
        /// Defines the date format to be used.
        /// </summary>
        public readonly static string DATE_FORMAT = "dd/MM/yyyy";
        public readonly static int MAX_LIST_OPTIONS = 9;
    }
}
